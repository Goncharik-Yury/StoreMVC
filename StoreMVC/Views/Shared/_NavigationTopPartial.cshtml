<div class="navbar navbar-inverse navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			@Html.ActionLink("StoreMVC", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
		</div>

		<div class="navbar-collapse collapse">
			<ul class="nav navbar-nav">
				<li>@Html.ActionLink("Products", "Index", "Products", new { categoryNameToSearch = "all" }, null)</li>

				@if (User.IsInRole("Moderator") || User.IsInRole("Admin"))
				{
					<li>@Html.ActionLink("Orders", "Index", "Orders")</li>
				}

				@if (User.IsInRole("Admin"))
				{
					<li>@Html.ActionLink("Customers", "Index", "UserProfiles")</li>
				}


				@*<li>@Html.ActionLink("Upload", "Upload", "Products")</li>*@
				<li>
					@using (Html.BeginForm("Index", "Products", FormMethod.Get))
					{
						<input type="text" name="productNameToSearch" placeholder="Product search..." class="form-control" style="margin-top:8px;" />
						@*<button type="submit" class="btn btn-default btn-primary">
					<span class="glyphicon glyphicon-search"></span>
					<span>Search</span>
				</button>*@
					}
				</li>
				@*<li>@Html.ActionLink("About", "About", "Home")</li>
		<li>@Html.ActionLink("Contact", "Contact", "Home")</li>*@
			</ul>

			<div id="login" class="navbar-form navbar-right">
				@Html.Partial("_LoginPartial")
			</div>
		</div>
	</div>
</div>