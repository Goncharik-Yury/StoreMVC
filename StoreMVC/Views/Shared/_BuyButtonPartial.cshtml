@model int

@if (WebSecurity.IsAuthenticated)
{
	@Html.ActionLink("Buy", "BuyPage", "Orders", new { orderId = Model }, new { @class = "btn btn-default", @id = "BuyForm", @onclick = "document.getElementById('BuyForm').submit();" })
}