@model IEnumerable<StoreMVC.Models.Product>

@foreach (var item in Model)
{
	<div class="row">
		<div class="col-md-3">
			<a href="@Url.Action("Details", new { id = item.ProductId })">
				<img src="@Html.DisplayFor(modelItem => item.imgName)" class="img-responsive img-rounded" />
			</a>
		</div>
		<div class="col-md-9">
			<b>@Html.ActionLink(item.Name, "Details", new { id = item.ProductId })</b>
			<div>
				@Html.DisplayFor(modelItem => item.Description)
			</div>
			<br />
			<div>
				<b>Цена: @Html.DisplayFor(modelItem => item.Price) p.</b>
			</div>
			@*<div>
					@Html.DisplayFor(modelItem => item.Category)
				</div>*@
			<br />
			<div>
				@*@if (User.IsInRole("Moderator") || User.IsInRole("Admin"))
					{*@
				@Html.ActionLink("Edit", "Edit", new { id = item.ProductId });
				@*@:|*@
				@*  }*@

				@*@if (User.IsInRole("Moderator") || User.IsInRole("Admin"))
					{*@
				@*@: |*@
				@Html.ActionLink("Delete", "Delete", new { id = item.ProductId });
				@*  }*@
			</div>
		</div>


	</div>
	<hr />
	<br />
}
